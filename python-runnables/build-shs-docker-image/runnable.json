/* This file is the descriptor for the python runnable build-shs-docker-image */
{
    "meta": {
        "label": "Build Spark History Server Docker Image",
        "description": "",
        "icon": "icon-picture" // https://fontawesome.com/v3.2.1/icons/
    },

    /* whether the runnable's code is untrusted */
    "impersonate": false,
    
    "paramsPythonSetup": "get_spark_base_images.py",
    "params": [
        {
            "name": "spark-base-image",
            "label": "Docker image",
            "description": "Select the spark docker image on which to base the history server.",
            "type": "SELECT",
            "getChoicesFromPython": true
        }
    ],
    
    "requiresGlobalAdmin": true,
    "permissions": ["ADMIN"],

    "macroRoles": [
        {
            "type": "PROJECT_MACROS"
        } 
    ]
}

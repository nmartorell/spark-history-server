/* This file is the descriptor for the python runnable stop-spark-history-server */
{
    "meta": {
        "label": "Stop Spark History Server",
        "description": "Stops the running Spark History Server (if any).",
        "icon": "icon-stop" // https://fontawesome.com/v3.2.1/icons/
    },

    /* whether the runnable's code is untrusted */
    "impersonate": false,

    "requiresGlobalAdmin": true,
    "permissions": ["ADMIN"],
    
    "resultType" : "HTML",
    "macroRoles": [
        {
            "type": "PROJECT_MACROS"
        } 
    ]
}
